<h1><%= @title %></h1>



<h1>Upload Document</h1>

<h1><%= @title %></h1>

<!--%= render :partial => 'activity_documents/document', :collection =>  [@user], :as => :user%--> 

<!--% form_for  @document, :url => activity_document_path,  :html => { :multipart => true } do |f| %-->
<%= form_for [@activity, @activity.activity_documents.build], :url => activity_document_path,  :html => { :multipart => true } do |f| %>
   
  
   <p>
    <label for="activity_documents">1 Upload document!:</label>
    <%= f.file_field :name %>
    
    <!--%= f.hidden_field :avatar_cache %-->
  </p>
     
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

